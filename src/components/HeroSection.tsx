'use client';

import React from 'react';

const HeroSection = () => {
  return (
    <section className="hero-content-section">
      {/* Hero content bo tukaj - za zdaj prazno */}
    </section>
  );
};

export default HeroSection;
